Para la compilación:

- Crear carpeta build en el directorio la2
- Dentro de build ejecutar: cmake ..
- Dentro de build ejecutar: make

Para ejecutar el algoritmo:

- Dentro de build ejecutar(ejemplo): ./la2 "argumentos"
