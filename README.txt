Para la compilación:

- Crear carpeta build en el directorio la1
- Dentro de build ejecutar: cmake ..
- Dentro de build ejecutar: make

Para ejecutar el algoritmo:

- Dentro de build ejecutar(ejemplo): ./la1 -t ../../datasetsPr1IMC/dat/train_xor.dat 
